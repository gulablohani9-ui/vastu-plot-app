name: vastu_plot_app
description: "Vastu Plot Mapping and PDF Generator"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  pdf: ^3.10.8
  printing: ^5.11.1
  image_picker: ^1.0.7
  path_provider: ^2.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/
